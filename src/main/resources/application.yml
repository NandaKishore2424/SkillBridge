spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/skillbridge_db
    username: postgres
    password: 123Sumathi@@@
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  sql:
    init:
      mode: always

server:
  port: 8080